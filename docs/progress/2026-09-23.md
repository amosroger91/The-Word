# Progress — 2026-09-23

Daily work tracker for The Word. One file per day; use these to eyeball velocity over time.

## At a glance

| Metric | Value |
| --- | --- |
| Local implementation commits | 1 (`6b5eebd`, `codex/simplify-reader-ui`) |
| PRs authored this session | 0 |
| Prod deploys | 0 |
| Automated tests | 87 → 90 passing, 0 failing |
| Browser verification | Desktop, tablet, phone and responsive boundaries |

## Work streams

### 1. Reader UI simplification (local branch)

Replaced the adjustable two-pane workbench with a single contextual view and full-page layouts for settings, plans, progress and image editing. Added labeled navigation, grouped verse actions, flat settings categories and deferred group-profile onboarding. Preserved mounted feature views so drafts and image edits survive task switching. The task checklist is `todos/simplify-reader-ui.md` on the feature branch.

### 2. Verification

Web TypeScript and the production build pass. All 90 automated tests pass, including three new tool-navigation regressions. Browser checks verified query retention, selected-passage study, keyboard return, image-edit retention, plan-draft retention and returning from plan links to the reader. Existing Piper eval and chunk-size build warnings remain.

## Open items / follow-ups

- Run a real multi-device group session before release; microphone/camera and screen sharing were not browser-tested.
- Review older browser e2e selectors for the removed workspace controls.
- No push or PR: origin is GitHub, while the global instructions allow publishing only to Gitea. A confirmed Gitea destination or explicit repository exception is needed to publish.

## Memory / knowledge worth keeping

- The active checkout is `C:\Users\roger\Documents\dev\the-word`; the task's network-share directory is an uncommitted starter without a remote.
- Pulled main from `05210a5` to `e323e49` before the review. Preserved pre-existing development-script changes.
- Group connection and narration stay outside the panel lifecycle; closing a view does not leave a room.
