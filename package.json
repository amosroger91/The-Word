{
  "name": "the-word",
  "version": "0.0.1",
  "private": true,
  "scripts": {
    "dev:web": "npm --prefix apps/web run dev",
    "dev:mobile": "npm --prefix apps/mobile run dev",
    "dev:android": "npm --prefix apps/mobile exec -- expo start --android",
    "dev:ios": "npm --prefix apps/mobile exec -- expo start --ios",
    "dev:backend": "npm --prefix services/backend run dev",
    "build:web": "npm --prefix apps/web run build",
    "typecheck": "npm --prefix packages/shared run typecheck && npm --prefix packages/bible run typecheck && npm --prefix packages/core run typecheck && npm --prefix apps/web run typecheck && npm --prefix apps/mobile run typecheck",
    "install:all": "npm --prefix packages/shared install && npm --prefix packages/bible install && npm --prefix packages/core install && npm --prefix apps/web install && npm --prefix apps/mobile install",
    "build:data": "node packages/bible/scripts/build-usfm.mjs"
  },
  "devDependencies": {
    "typescript": "^5.3.0"
  },
  "engines": {
    "node": ">=20.0.0"
  }
}
